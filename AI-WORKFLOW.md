# Website workflow

Read project context and current upstream brand/voice. Inspect this repository's Git status separately from the parent lab. Make scoped static edits; catalogue/aggregates live in public/lab-data.js.

Run scripts/verify.ps1 and inspect the actual page at mobile/desktop sizes, keyboard and reduced motion. Update maintenance docs when behaviour changes.

Show the concrete candidate before release. A push to remote main triggers production. No deployment or live routing change before review. Old Riptide bilingual/ocean requirements were superseded by the approved rebrand.
